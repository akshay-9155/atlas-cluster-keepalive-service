{
  "name": "cron-service",
  "version": "1.0.0",
  "description": "A cron-service that keeps mongodb free teir cluster alive so that it doesn't gets paused due to in activity.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/server.js",
    "start": "node src/server.js"
  },
  "author": "Akshay Anand",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.14.0",
    "dotenv": "^17.4.0",
    "express": "^5.2.1",
    "node-cron": "^4.2.1",
    "nodemon": "^3.1.14",
    "p-retry": "^7.1.1"
  }
}
